{
  "name": "710-wiki",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "quartz-site"
  ],
  "scripts": {
    "validate:canvases": "node scripts/validate-canvases.mjs",
    "build": "npm run validate:canvases && npm run build -w quartz-site",
    "start": "npx serve quartz-site/public -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1",
    "yaml": "^2.6.0"
  }
}
