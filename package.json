{
  "name": "710-wiki",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm install --prefix quartz-site && npm run build --prefix quartz-site",
    "start": "npx serve quartz-site/public -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
