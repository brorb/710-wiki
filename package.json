{
  "name": "710-wiki",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate:canvases": "node scripts/validate-canvases.mjs",
    "build": "npm run validate:canvases && npm install --prefix quartz-site && npm run build --prefix quartz-site",
    "start": "npx serve quartz-site/public -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1",
    "yaml": "^2.6.0"
  }
}
